1. Each part of an instruction is represented by a number, placing these numbers together forms an instruction. Instructions are stored as numbers in the hardware.
2.  the instruction  
	 add x9, x20, x21       ; can be represented as
	 ![[Pasted image 20260914110429.png]]Each segment is known as [[field]].  The 2nd and 3rd fields are x21 and x20, 5th field is x9.
	 this way of writing instructions is [[instruction format]] , containing fields of binary numbers.
	 RISC-V instructions are 32 bit long. 
	 
	 The numeric version of instructions is called [[machine language]], which is the binary representations.
	 Instead of writing binary numbers we can use hexadecimal numbers.

## RISCV Fields: 

### R-Type

![[Pasted image 20260920134524.png]]

1. ==opcode== represents the operation of the instruction, along with additional fields like funct3 and funct7. rs1 and rs2 are reg source operands and rd is the reg destination operand.
### I-Type

In order to accommodate more bits for instructions like load, which use memory address, instead of changing length, we change the field division of the instruction leading to types. As for address, 5 bits will be too small allowing only 32 addresses, which lead to field extension. The following is the design principle related to it:
			Design Principle 3: Good design demands good compromises

The 12 bit immediate can represent from -2^11 to 2^11 the constant used in the instruction.
![[Pasted image 20260920134635.png]]

### S-Type

![[Pasted image 20260920134702.png]]





![[Pasted image 20260920142507.png]]

